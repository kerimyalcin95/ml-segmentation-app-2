export const documentSize = $state({
    width: 0,
    height: 0,
});

export const workspaceSize = $state({
    width: 0,
    height: 0,
});

export const documentCrop = $state({
    x: 0,
    y: 0,
    width: 0,
    height: 0
});